# nested functions calls = functions inside functions calls = functions inside functions calls

#num = input("Enter a number: ")
#num = float(num)
#num = abs(num)
#num = round(num)
#print(num)
# Wrong way to do it:

print(round(abs(float(input("Enter a number: ")))))